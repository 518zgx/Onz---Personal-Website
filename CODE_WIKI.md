# ONZ 个人网站 Code Wiki

## 文档信息

- **项目名称**: ONZ 个人网站 (Onz-Personal-Website)
- **文档版本**: 2.0
- **创建日期**: 2026-05-16
- **最后更新**: 2026-05-22

---

## 1. 项目概述

### 1.1 项目简介

ONZ 个人网站是一个采用赛博朋克美学风格的静态个人作品集网站。该项目展示了创意编程与生成艺术的个人品牌，通过精美的视觉效果和流畅的用户体验展现数字创作者的专业形象。网站采用纯前端技术栈实现，无需后端服务器，支持直接通过浏览器打开运行。

### 1.2 核心价值

项目体现了以下核心设计理念：首先强调视觉冲击力，通过故障艺术效果（Glitch Art）、动态粒子系统和全息渐变等元素营造强烈的科技感氛围；其次注重用户体验，通过精心设计的入场动画、过渡效果和响应式布局确保跨设备的流畅体验；最后追求代码质量，整个项目使用原生 JavaScript 和 Canvas API 实现复杂的视觉效果，无需依赖任何外部库或框架。

### 1.3 目标用户

本项目主要面向以下几类用户：创意编程爱好者可以通过项目学习如何用纯前端技术实现复杂的视觉效果；Web 开发者能够参考代码结构和动画实现方式；设计师可以参考品牌视觉系统的设计方法；同时项目也适合作为个人作品集网站的模板进行二次开发。

---

## 2. 项目架构

### 2.1 目录结构

```
e:\AI\Onz-个人网页/
└── personal-website/
    ├── onz-brand-philosophy.md    # 品牌视觉哲学文档
    ├── onz-intro.html             # 引导页/着陆页
    ├── onz-main.html              # 主页面/作品集页
    ├── games.html                 # 游戏中心页面
    └── toolkit.html               # 创意工具箱页面
```

### 2.2 文件架构说明

| 文件名 | 类型 | 职责 | 页面层级 |
|--------|------|------|----------|
| onz-brand-philosophy.md | Markdown 文档 | 存储品牌视觉哲学、色彩系统、字体系统等设计规范 | 文档层 |
| onz-intro.html | HTML 页面 | 作为网站入口页面，包含品牌展示和加载动画 | 第一层 |
| onz-main.html | HTML 页面 | 主作品集页面，包含导航、关于、项目展示、联系方式等模块 | 第二层 |
| games.html | HTML 页面 | 游戏中心页面，包含 6 个互动小游戏 | 第三层 |
| toolkit.html | HTML 页面 | 创意工具箱页面，包含 6 个创意工具 | 第三层 |

### 2.3 页面流转关系

用户访问网站时的页面流转顺序如下：首先用户通过浏览器打开 `onz-intro.html` 进入引导页；引导页展示 ONZ 品牌 LOGO 动画和加载进度；用户点击"ENTER"按钮后，通过过渡动画无缝跳转至 `onz-main.html` 主页面；主页面包含完整的作品集内容，用户可浏览关于信息、项目案例、生成艺术展示并通过导航或社交链接进行互动；点击"Game"项目卡片跳转到 `games.html`；点击"创意工具箱"项目卡片跳转到 `toolkit.html`。

---

## 3. 技术栈分析

### 3.1 核心技术

本项目采用纯原生技术栈实现，不依赖任何外部 JavaScript 框架或库。HTML5 用于语义化页面结构，通过 Canvas API 实现粒子系统、游戏渲染和创意工具；CSS3 负责样式设计和动画表现；JavaScript 处理交互逻辑、Canvas 渲染和游戏逻辑。同时项目使用了 Google Fonts 在线字体服务，引入 Orbitron、Rajdhani 和 Space Grotesk 三种字体。

### 3.2 外部依赖

| 依赖名称 | 版本 | 用途 | 加载方式 |
|---------|------|------|----------|
| Google Fonts | 最新 | 提供 Orbitron、Rajdhani、Space Grotesk 字体 | CDN 外链 |

### 3.3 浏览器兼容性

项目支持的浏览器版本如下：Chrome 80 及以上版本完全支持所有功能；Firefox 75 及以上版本支持全部特性；Safari 13 及以上版本支持核心效果；Edge 80 及以上版本与 Chrome 保持一致。不支持 Internet Explorer 浏览器。

---

## 4. 引导页详解（onz-intro.html）

### 4.1 页面结构

引导页是用户进入网站的第一触点，承担着品牌形象展示和加载体验的双重职责。页面结构包含三个核心层：Canvas 动画层作为背景持续播放粒子效果；品牌展示层居中显示 ONZ LOGO 和故障艺术效果；UI 交互层包含进度条、加载文字和进入按钮等元素。

### 4.2 核心功能模块

**粒子系统模块**负责创建和管理两种类型的粒子。爆炸粒子（explosion type）在初始化时随机分配角度和速度，具有生命周期衰减特性，绘制时带有发光效果和阴影模糊。网格粒子（grid type）在画布上随机分布，垂直方向运动并在边界处循环，透明度随时间周期性变化。

**故障艺术效果模块**通过 CSS 伪元素实现文字故障效果。glitch-layer-1 和 glitch-layer-2 分别控制文字的不同偏移部分，配合 clip-path 属性裁剪显示区域，通过 keyframes 动画改变 transform 属性产生位移效果。

**加载序列控制模块**管理整个引导页的时序逻辑。createIntroSequence 函数定义了一个包含 8 个时间节点的事件序列，分别触发不同位置的粒子爆炸效果。进度条在 3 秒内从 0% 增长到 100%，完成后隐藏进度条和加载文字，最后在 3.5 秒时显示进入按钮。

**页面过渡模块**实现无缝页面跳转。使用 `fetch()` 预加载主页面内容，通过 `document.write()` 直接替换文档内容，避免浏览器重新加载导致的黑屏闪烁。

### 4.3 关键类说明

**Particle 类**是粒子系统的核心类，包含三个主要属性：x 和 y 坐标属性用于定位，type 类型属性区分爆炸粒子和网格粒子。update 方法根据粒子类型更新位置和状态，draw 方法根据类型使用不同的绘制策略。爆炸粒子使用圆弧绘制并应用透明度衰减，网格粒子使用矩形绘制并应用周期性透明度。

---

## 5. 主页面详解（onz-main.html）

### 5.1 页面布局

主页面采用经典的作品集网站布局结构。顶部固定导航栏包含 LOGO 和导航链接（支持移动端汉堡菜单）；Hero 区域占据首屏高度，展示品牌标题和核心信息；统计栏以数据卡片形式展示关键指标；随后依次为关于、项目、艺术展示和联系方式等内容区块；底部为简约页脚。

### 5.2 主要模块

**导航模块**实现了固定导航栏效果，具有毛玻璃背景（backdrop-filter: blur）、渐入动画和悬停状态变化。导航链接支持平滑滚动定位到对应区块，移动端自动切换为汉堡菜单。

**英雄区域模块**展示了品牌主标题，包含字母级别的颜色样式定义和文本阴影效果。标题下方包含副标题和两个行动按钮（CTA），支持响应式垂直堆叠。

**统计栏模块**使用 CSS Grid 布局展示四个统计指标，数字使用渐变色文字效果。统计栏采用淡入动画延迟出现，支持响应式 2×2 网格布局。

**关于模块**采用两栏布局，左侧为自我介绍文字描述，右侧为技能卡片网格。每个技能卡片包含左侧渐变边框装饰和悬停上浮效果。

**项目展示模块**使用响应式网格布局展示三个项目卡片：个人网页（当前页面）、Game（游戏中心）、创意工具箱。每个卡片包含顶部渐变色条装饰、项目图标区域、标题描述和标签列表。卡片支持悬停时上浮和阴影增强效果。

**生成艺术模块**展示创意实验室内容，左侧为交互式体验入口，右侧为艺术理念说明文字。整体采用双栏布局。

**联系方式模块**包含社交媒体图标链接和邮箱联系入口。社交图标采用圆形边框设计，悬停时背景填充和颜色反转。

### 5.3 JavaScript 功能

主页面包含三个主要的 JavaScript 功能模块。粒子背景模块创建 80 个粒子随机分布在画布上，粒子间距离小于 150 像素时绘制连接线，营造动态网络效果，支持鼠标跟随和右键散开交互。滚动动画模块使用 IntersectionObserver API 监听元素进入视口，添加可见类触发动画。平滑滚动模块为所有锚点链接添加点击事件监听，实现平滑滚动定位。

---

## 6. 游戏中心详解（games.html）

### 6.1 页面布局

游戏中心页面采用卡片式网格布局展示 6 个游戏，点击卡片进入对应游戏的全屏画布界面。页面包含顶部标题区域、游戏卡片网格、游戏画布区域（隐藏状态）、HUD 信息栏和游戏结束弹窗。

### 6.2 游戏列表

| 游戏名称 | 类型 | 描述 | 操作方式 |
|----------|------|------|----------|
| 贪吃蛇 | 经典游戏 | 使用方向键或 WASD 控制蛇移动，吃到食物得分 | 键盘方向键/WASD/滑动手势 |
| 打砖块 | 经典游戏 | 控制挡板反弹小球击碎砖块 | 鼠标/方向键/触摸拖动 |
| 太空射击 | 射击游戏 | 控制飞船消灭敌机，获取道具 | 键盘/WASD/空格射击 |
| 记忆翻牌 | 益智游戏 | 翻开卡牌找到配对的图案 | 鼠标点击/触摸 |
| 2048 | 益智游戏 | 滑动合并相同数字方块 | 方向键/WASD/滑动手势 |
| 赛博飞鸟 | 休闲游戏 | 控制飞鸟穿越管道 | 空格键/鼠标点击/触摸 |

### 6.3 核心游戏机制

**贪吃蛇**：包含输入缓冲队列、暂停功能（P 键）、食物脉动动画、死亡闪烁效果、吃食物粒子特效和蛇身圆角绘制。

**打砖块**：支持鼠标和键盘控制、球速度归一化、球拖尾效果、道具系统（加宽挡板、加命）、砖块破碎粒子和球发射机制。

**太空射击**：包含敌机子弹系统、3 种敌机类型（直线型、蛇行型、坦克型）、道具掉落（护盾、三连发）、连杀奖励和屏幕震动效果。

**记忆翻牌**：包含翻牌动画、计时器、连击奖励、配对成功粒子和通关时间奖励。

**2048**：包含新方块弹入动画、合并闪烁效果、最佳分数记录（localStorage）和圆角方块绘制。

**赛博飞鸟**：包含难度递增机制、飞鸟旋转角度、地面滚动、死亡闪烁效果和未开始时浮动动画。

### 6.4 通用游戏框架

**游戏状态管理**：使用 `launchGame()` 初始化游戏，`exitGame()` 返回游戏选择界面，`restartCurrentGame()` 重新开始当前游戏。

**资源清理**：`clearAllGameResources()` 负责清理动画循环和事件监听，防止内存泄漏。

**触控支持**：所有游戏支持触摸操作，通过 `canvasCoords()` 函数处理 CSS 缩放后的坐标映射。

---

## 7. 创意工具箱详解（toolkit.html）

### 7.1 页面布局

创意工具箱页面采用卡片式网格布局展示 6 个创意工具，点击卡片进入对应工具的全屏界面。页面包含顶部标题区域、工具卡片网格、工具界面区域（隐藏状态）和返回按钮。

### 7.2 工具列表

| 工具名称 | 类型 | 描述 | 核心功能 |
|----------|------|------|----------|
| 流场生成器 | 生成艺术 | Perlin 噪声驱动的粒子流场艺术 | 参数调节、配色模式、导出 PNG |
| 像素画编辑器 | 绘图工具 | 32×32 像素网格画布 | 画笔、橡皮擦、填充、导出 |
| 故障艺术工坊 | 文字效果 | 赛博朋克故障艺术生成器 | RGB 偏移、扫描线、噪点、导出 |
| 智能调色板 | 配色工具 | 基于色彩理论的调色板生成器 | 5 种和谐模式、复制颜色值、CSS 变量代码 |
| 分形探索器 | 数学艺术 | Mandelbrot 分形实时渲染 | 缩放、平移、配色、导出 |
| 波形合成器 | 动画工具 | 正弦波叠加可视化工具 | 参数调节、CSS @keyframes 代码生成 |

### 7.3 核心工具功能

**流场生成器**：使用 Perlin 梯度噪声生成平滑的粒子运动轨迹，支持粒子数量、噪声缩放、速度调节和多种配色模式。

**像素画编辑器**：支持画笔、橡皮擦和填充工具，16 色快捷调色板，自定义颜色选择器，支持导出原始像素分辨率 PNG。

**故障艺术工坊**：实时生成赛博朋克风格故障文字效果，可调 RGB 偏移量、扫描线密度和噪点强度。

**智能调色板**：基于 HSL 色彩空间自动生成和谐配色方案，支持互补色、三角色、类似色、分裂互补和四角色模式。

**分形探索器**：实时渲染 Mandelbrot 分形，支持鼠标滚轮缩放、拖拽平移和多种配色方案。

**波形合成器**：叠加 3 条正弦波创造独特波形图案，每条波可独立调节频率和振幅，自动生成 CSS @keyframes 动画代码。

---

## 8. 关键函数详解

### 8.1 引导页核心函数

**resize 函数**负责响应式画布尺寸调整。该函数获取窗口的 innerWidth 和 innerHeight 属性，将值同时赋给 canvas 元素的 width 和 height 属性以及局部变量，确保 Canvas 尺寸与可视区域保持同步。函数在页面加载时调用一次，并通过 resize 事件持续监听窗口变化。

**createExplosion 函数**用于在指定位置创建粒子爆炸效果。函数接收三个参数：x 和 y 坐标确定爆炸中心点，count 参数指定创建的粒子数量，默认为 100。函数内部循环创建指定数量的 Particle 类实例，并将 type 参数设置为爆炸类型。

**drawGrid 函数**绘制动态网格背景。函数首先设置描边颜色为青色，透明度为 0.1，然后遍历画布绘制垂直和水平网格线。水平线应用正弦函数实现波动偏移效果，创造动态感。函数使用 perspective 参数控制波动幅度，time 变量基于当前时间更新实现动画。

**drawCentralOrb 函数**绘制中心脉动圆环效果。该函数创建三个同心圆环，圆环半径基于屏幕尺寸计算并应用正弦波动。三个圆环使用不同的颜色，透明度依次递减（0.3、0.2、0.1），并使用虚线样式（setLineDash）增强视觉效果。

**createIntroSequence 函数**定义引导页的完整加载序列。函数创建一个包含 8 个时间节点的事件数组，时间点分别为 0ms、500ms、800ms、1100ms、1500ms、2000ms、2300ms 和 2800ms，每个时间点对应不同的粒子爆炸位置和数量。序列执行时使用 setTimeout 延迟触发，同时启动进度条更新函数和粒子动画。

**window.enterMain 函数**处理进入主页面的事件。该函数首先停止 Canvas 动画，然后依次激活扫描线效果、过渡遮罩层，创建五次连续爆炸效果增强过渡体验，使用 fetch() 预加载主页面，最后通过 document.write() 无缝替换文档内容。

### 8.2 主页面核心函数

**Particle 类**构造函数随机初始化位置、大小、速度和颜色属性。update 方法实现边界循环逻辑，当粒子移出画布时从对侧重新进入，同时实现鼠标跟随引力和右键散开效果。draw 方法绘制带透明度的圆形粒子。

**animate 函数**为主页面粒子背景的动画循环。函数首先清除画布，然后更新并绘制所有粒子。接下来实现粒子间连接逻辑，遍历所有粒子对，计算距离后对距离小于 150 像素的粒子对绘制连接线，透明度根据距离线性衰减。最后使用 requestAnimationFrame 实现循环调用。

**IntersectionObserver 回调函数**处理滚动动画触发。当观测到的元素进入视口且交叉比例超过 10%（threshold: 0.1）时，为该元素添加 visible 类名，触发淡入上浮的 CSS 动画。

### 8.3 游戏中心核心函数

**launchGame(type)** 根据游戏类型初始化对应游戏的 Canvas 和逻辑。

**clearAllGameResources()** 清理所有游戏资源，包括动画循环、事件监听和 Canvas 状态。

**canvasCoords(e)** 将鼠标/触摸坐标转换为 Canvas 内部坐标，处理 CSS 缩放影响。

**showGameOver(title)** 显示游戏结束弹窗，包含重新开始和返回按钮。

### 8.4 创意工具箱核心函数

**openTool(name)** 打开指定工具的界面，设置工具页面显示状态。

**closeTool()** 关闭当前工具界面，清理动画循环和界面状态。

**copyText(text)** 复制文本到剪贴板，并显示 toast 通知。

**getCanvasDims(width, height)** 根据屏幕尺寸动态计算 Canvas 尺寸，保持宽高比。

---

## 9. CSS 设计系统

### 9.1 色彩系统

项目定义了完整的赛博朋克风格色彩系统。主色调包含极夜黑（#0a0a0f）作为背景色、电光青（#00f5ff）作为主强调色、烈焰橙（#ff4500）作为次强调色、极光紫（#9d00ff）作为辅助强调色、矩阵绿（#00ff88）作为点缀色。辅助色调包含金属银（#c0c0c0）用于正文文字、暗夜蓝（#1a1a2e）用于卡片背景。

### 9.2 字体系统

字体使用遵循三档权重体系。标题字体采用 Orbitron，这是一种几何感强的无衬线字体，字重范围 400-900，适用于品牌 LOGO、章节标题和数字展示。辅助字体使用 Rajdhani，字重范围 300-700，适合正文和导航文字。正文字体采用 Space Grotesk，字重范围 300-700，用于段落和说明文字。

### 9.3 动画系统

项目实现了多种动画效果。glitch 动画通过 transform 属性的 translate 变化创造文字抖动效果。fadeInUp 动画实现元素从下方淡入的过渡效果。scanline 动画使用线性渐变背景实现扫描线移动效果。pulse 动画实现元素的脉动缩放效果。letterGlitch 动画为 LOGO 字母创造轻微的随机偏移。entryTransition 动画使用 clip-path 属性实现圆形扩散的入场过渡效果。

### 9.4 响应式断点

| 断点 | 设备类型 | 列数（卡片网格） | 主要调整 |
|------|----------|------------------|----------|
| ≥1400px | 大屏桌面 | 3 | 最大宽度限制，居中布局 |
| ≤968px | 平板 | 2 | 导航切换为汉堡菜单 |
| ≤768px | 小平板 | 2 | 触控目标 44px |
| ≤480px | 手机 | 1 | 触控目标 48px，文字最小 14px |

---

## 10. 依赖关系图

### 10.1 外部资源依赖

项目对外部资源的依赖降至最低。Google Fonts 提供三种字体的在线加载，通过 link 标签预连接优化加载性能。字体加载采用 preconnect 指令提前建立连接，跨域属性设置为允许跨域请求。

### 10.2 内部文件依赖

```
onz-intro.html ──(跳转)──> onz-main.html
                               │
                               ├──(跳转)──> games.html
                               │
                               └──(跳转)──> toolkit.html
```

引导页通过 `document.write()` 无缝过渡到主页面；主页面通过锚点链接跳转到游戏中心和创意工具箱；游戏中心和工具箱通过返回按钮返回主页面。

### 10.3 模块间关系

Canvas 渲染模块独立运行，不依赖 DOM 结构；UI 交互模块依赖 DOM 元素和事件系统；动画系统依赖 CSS transition 和 keyframes；响应式布局依赖 CSS Media Queries 和 JavaScript resize 事件；游戏和工具模块共享通用的 Canvas 资源管理机制。

---

## 11. 项目运行方式

### 11.1 本地运行

由于项目为纯静态网站，无需任何构建工具或服务器即可运行。直接使用浏览器打开 onz-intro.html 文件即可启动网站。所有资源（字体、图片）通过 CDN 或内联方式加载，无需额外配置。

### 11.2 本地服务器（可选）

如需使用本地开发服务器，推荐使用以下任一方式。使用 Python 启动简单服务器，在项目目录执行 `python -m http.server 8000` 后访问 http://localhost:8000。使用 Node.js 的 http-server 包，执行 `npx http-server` 启动服务器。使用 VS Code 的 Live Server 扩展，右键点击 onz-intro.html 选择"Open with Live Server"。

### 11.3 部署方式

静态网站可部署至多种平台。GitHub Pages 在仓库设置中启用 Pages 功能，选择 main 分支即可托管。Vercel 和 Netlify 提供拖拽部署功能，可直接上传项目文件夹。云对象存储（如阿里云 OSS、腾讯云 COS）配置静态网站托管模式后上传文件即可。

---

## 12. 扩展开发指南

### 12.1 添加新页面

如需添加新页面，复制 onz-main.html 作为模板，保留顶部的字体加载 link 标签和 CSS 变量定义区域。在 onz-main.html 的项目卡片中添加新页面的跳转链接。确保新页面遵循现有的 CSS 类命名规范和结构约定。

### 12.2 添加新游戏

在 games.html 中添加新的游戏卡片和对应的游戏逻辑。创建游戏初始化函数，实现游戏主循环，添加事件监听和资源清理逻辑。确保遵循现有的游戏框架模式。

### 12.3 添加新工具

在 toolkit.html 中添加新的工具卡片和对应的工具界面。创建工具初始化函数，实现工具的核心功能，添加参数调节控件和导出功能。确保遵循现有的工具框架模式。

### 12.4 自定义色彩

修改页面中 :root 选择器内的 CSS 变量可以快速调整整体配色。将 --onz-cyan 改为其他颜色值即可将主强调色全局替换。其他变量同样支持独立自定义。

### 12.5 粒子效果调优

调整引导页的 Particle 类构造函数参数可改变爆炸效果。修改 speed 范围值改变粒子飞行速度，修改 size 范围值改变粒子大小，修改 decay 范围值改变粒子生命周期。调整 initGridParticles 函数的粒子数量可改变背景密度。

---

## 13. 性能注意事项

### 13.1 Canvas 渲染优化

粒子数量直接影响渲染性能，引导页爆炸效果最多同时存在约 200 个粒子，主页面保持 80 个粒子的恒定数量。在低端设备上可适当减少粒子数量以保持流畅度。使用 requestAnimationFrame 确保动画与浏览器刷新率同步。

### 13.2 字体加载优化

项目使用 font-display: swap 策略避免字体加载阻塞页面渲染。预连接指令提前建立网络连接。字体请求设置跨域属性确保正确缓存。

### 13.3 动画性能

所有动画优先使用 transform 和 opacity 属性，这两种属性的变化不会触发重排（reflow），浏览器可进行硬件加速。避免在动画中使用 box-shadow 等触发重绘的属性。

### 13.4 响应式图像

所有 Canvas 元素使用 CSS max-width: 100% 和 height: auto 确保在小屏幕上正确缩放。游戏和工具使用 JavaScript 动态计算 Canvas 尺寸以适应不同屏幕。

---

## 14. 维护建议

### 14.1 代码组织

当前项目将 HTML、CSS 和 JavaScript 集中在单个文件中。对于更大规模的项目，建议将 CSS 和 JavaScript 分离到独立文件，使用 link 和 script 标签引用。JavaScript 代码可进一步模块化，将功能相关的函数分组组织。

### 14.2 文档维护

品牌哲学文档（onz-brand-philosophy.md）应与实际实现保持同步。修改色彩、字体或视觉风格时，同步更新文档记录。添加新功能时补充相应的使用说明。

### 14.3 版本控制

建议使用 Git 进行版本控制，保留完整的修改历史。重要功能变更时创建版本标签（Tag），便于追踪项目演进和回溯历史版本。

---

## 15. 附录

### 15.1 文件快速索引

| 文件路径 | 行数 | 主要内容 |
|----------|------|----------|
| onz-intro.html | ~800 | 引导页完整实现 |
| onz-main.html | ~1500 | 主页面完整实现 |
| games.html | ~1800 | 游戏中心完整实现 |
| toolkit.html | ~1200 | 创意工具箱完整实现 |
| onz-brand-philosophy.md | 77 | 品牌设计规范 |

### 15.2 关键选择器速查

| 选择器 | 用途 |
|--------|------|
| .onz-text | 品牌 LOGO 文字容器 |
| .glitch-layer-1/2 | 故障艺术效果层 |
| .onz-underline | LOGO 下方渐变线条 |
| .enter-btn | 进入按钮容器 |
| .transition-overlay | 页面过渡遮罩层 |
| .scanline | CRT 扫描线效果 |
| .particles-bg | 主页面粒子背景 Canvas |
| .hero-title | 英雄区域主标题 |
| .project-card | 项目卡片容器 |
| .skill-card | 技能卡片组件 |
| .game-card | 游戏卡片容器 |
| .tool-card | 工具卡片容器 |
| .game-canvas | 游戏画布 |
| .tool-canvas | 工具画布 |

### 15.3 全局函数清单

| 函数名 | 文件 | 功能 |
|--------|------|------|
| resize | onz-intro.html | 画布尺寸响应式调整 |
| Particle | onz-intro.html | 粒子类定义 |
| createExplosion | onz-intro.html | 创建粒子爆炸 |
| drawGrid | onz-intro.html | 绘制动态网格 |
| drawCentralOrb | onz-intro.html | 绘制中心脉动圆环 |
| createIntroSequence | onz-intro.html | 引导序列控制 |
| showEnterButton | onz-intro.html | 显示进入按钮 |
| window.enterMain | onz-intro.html | 页面跳转处理 |
| animate | onz-main.html | 粒子动画循环 |
| handleTransition | onz-main.html | 入场动画处理 |
| launchGame | games.html | 启动指定游戏 |
| exitGame | games.html | 退出游戏返回列表 |
| canvasCoords | games.html | 坐标转换 |
| openTool | toolkit.html | 打开指定工具 |
| closeTool | toolkit.html | 关闭工具 |
| copyText | toolkit.html | 复制文本到剪贴板 |

---

## 文档更新日志

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2026-05-16 | 1.0 | 初始版本，完成项目全面分析 |
| 2026-05-22 | 2.0 | 新增游戏中心和创意工具箱文档，更新响应式布局和触控支持说明 |

---

*本文档由 AI 辅助生成，如有问题请联系项目维护者。*